import styled, {css} from "styled-components";

export const StyledCartItemCustomization = styled.ul(() => css`
    list-style: none;
    padding: 0;
    margin: .5rem 0 0;
    font-style: italic;
    font-size: .875rem;
`)

export const StyledCartItemCustomizationItem = styled.li(() => css`
    //
`)