import styled, {css} from "styled-components";

export const StyledCheckoutPageReview = styled.div(() => css`
    display: flex;
    flex-direction: column;
    background: #FFF;
`)

export const StyledCheckoutPageForm = styled.form(() => css`
    display: flex;
    flex-direction: column;
    background: #FFF;
    padding: 1rem;
`)